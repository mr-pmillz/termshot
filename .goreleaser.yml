---
version: 2

builds:
- env:
  - CGO_ENABLED=0
  goos:
  - linux
  - darwin
  goarch:
  - amd64
  - arm64
  main: ./cmd/termshot/main.go
  flags:
  - -trimpath
  ldflags:
  - -s -w -extldflags "-static" -X github.com/mr-pmillz/termshot/internal/cmd.version={{.Version}}
  mod_timestamp: '{{ .CommitTimestamp }}'

checksum:
  name_template: 'checksums.txt'

snapshot:
  version_template: "{{ .Tag }}-next"

release:
  name_template: "{{.ProjectName}} release v{{.Version}}"

changelog:
  disable: false
  use: github
  filters:
    exclude:
    - "Merge pull request"
    - "dependabot"

